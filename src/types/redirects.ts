// Generated from the v1 OpenAPI spec (`components.schemas`) — see
// scripts/gen-types.ts.
import type { Schemas } from './_spec';

/**
 * The result of resolving a path against the board's configured redirects
 * (`board.redirects.resolve()`). A headless frontend 308s to `target`, or
 * 404s when it's `null`.
 */
export type RedirectResolution = Schemas['RedirectResolution'];
